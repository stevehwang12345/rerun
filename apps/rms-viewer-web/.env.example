# 비워 두면 브라우저 안에서 안전한 Mock API를 사용합니다.
VITE_RMS_API_BASE=

# 실제 백엔드 개발 서버를 Vite의 /api 및 /rerun 경로로 프록시합니다.
RMS_BACKEND_TARGET=http://127.0.0.1:8080

# Mock 모드를 명시적으로 고정할 때 사용합니다.
VITE_RMS_USE_MOCK=true
